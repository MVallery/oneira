import CustomText from '@/components/ui/Text';
import { View } from 'react-native';

const CallbackScreen = () => {
  const onSubmit = (data: any) => {};

  return (
    <View>
      <CustomText>Test callback</CustomText>
    </View>
  );
};

export default CallbackScreen;
