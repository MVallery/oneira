import CustomText from '@/components/ui/Text';
import { View } from 'react-native';

export const ConnectionSection = ({ editing }: { editing: boolean }) => {
  return (
    <View>
      <CustomText>Connection Section</CustomText>
    </View>
  );
};
